# Reproducibility status

Internal revision: **V4 — 2026-09-20**.

Completed checks in the controlled author package:

- 268/268 numerical and property checks passed;
- an alternate QR-based numerical implementation agreed to machine precision;
- a clean replay reproduced 21/21 generated CSV tables byte-for-byte;
- LaTeX cross-reference, font, and render checks passed;
- all reported principal figures/tables were regenerated from machine-readable outputs.

These checks establish computational reproducibility of the implemented analysis. They do not independently establish instrument accuracy, field-site independence, or geographic/temporal generalization.

A second Codex audit is optional and should be treated as computational corroboration rather than new experimental validation.
